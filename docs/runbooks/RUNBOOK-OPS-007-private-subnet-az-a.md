# Runbook: Private Subnet AZ-A

## Overview
Private Subnet AZ-A is a network segment within the 'App VPC' in Availability Zone A. Resources here do not have direct internet access; outbound connections are routed through a NAT Gateway.

## Prerequisites
- AWS CLI configured with appropriate permissions.
- IAM permissions to describe VPCs, Subnets, NAT Gateways, and route tables.

## Architecture
Located in AZ-A with CIDR block 10.0.11.0/24. Associated with a route table that directs internet-bound traffic to the NAT Gateway in AZ-A. Typically hosts application tiers like ECS services or RDS instances.

## Operations

### Scaling
Subnet scaling involves expanding the CIDR range or creating new subnets. This is a manual process performed as part of infrastructure planning and managed via IaC.

### Monitoring
Monitor network traffic using VPC Flow Logs. Key metrics involve assessing traffic between private subnets and to/from the NAT Gateway.

### Backup & Recovery
Subnets are network configurations managed via IaC. They are not individually backed up. Recreating the VPC or modifying the IaC will redefine subnets.

### Common Issues
### Resources in Private Subnet AZ-A Cannot Access Internet
- **Diagnosis**: Verify the route table associated with this subnet. Ensure it has a default route (0.0.0.0/0) pointing to the NAT Gateway in AZ-A.
- **Resolution**: Update the route table to include the correct route to the NAT Gateway.

## Rollback Procedure
1. Revert the IaC commit that modified the private subnet configuration.
2. Apply the changes using the IaC tool (e.g., `terraform apply`).
3. Verify outbound internet connectivity for resources within Private Subnet AZ-A.

## Contacts
Generated by CloudCrew AI — 2026-09-09