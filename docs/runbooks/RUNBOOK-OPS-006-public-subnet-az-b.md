# Runbook: Public Subnet AZ-B

## Overview
Public Subnet AZ-B is a segment within the 'App VPC' located in Availability Zone B. Resources within this subnet can have direct access to the internet via the Internet Gateway.

## Prerequisites
- AWS CLI configured with appropriate permissions.
- IAM permissions to describe VPCs and Subnets.

## Architecture
Located in AZ-B with CIDR block 10.0.2.0/24. It is associated with a route table that directs internet-bound traffic to the Internet Gateway. It typically hosts resources like Application Load Balancers.

## Operations

### Scaling
Subnet scaling involves expanding the CIDR range or creating new subnets. This is generally a manual process performed as part of infrastructure planning and managed via IaC.

### Monitoring
Monitor network traffic through associated route tables and security groups. VPC Flow Logs provide visibility into traffic originating or terminating in this subnet.

### Backup & Recovery
Subnets are network configurations and are not 'backed up' in the traditional sense. Their state is managed by the VPC configuration within the IaC. Recreating the VPC or modifying IaC will redefine subnets.

### Common Issues
### Resources in Public Subnet AZ-B Cannot Reach Internet
- **Diagnosis**: Check the route table associated with this subnet. Ensure it has a default route (0.0.0.0/0) pointing to the Internet Gateway.
- **Resolution**: Update the route table to include the necessary route to the IGW.

## Rollback Procedure
1. Revert the IaC commit that modified the public subnet configuration.
2. Apply the changes using the IaC tool (e.g., `terraform apply`).
3. Verify network accessibility for resources within Public Subnet AZ-B.

## Contacts
Generated by CloudCrew AI — 2026-09-09