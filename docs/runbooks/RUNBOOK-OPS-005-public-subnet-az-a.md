# Runbook: Public Subnet AZ-A

## Overview
Public Subnet AZ-A resides in Availability Zone 'a' and is configured to allow resources within it to have direct or indirect access to the internet via the Internet Gateway. It typically hosts resources like NAT Gateways and Application Load Balancers.

## Prerequisites
- AWS CLI configured with appropriate credentials.
- IAM permissions to describe VPC, Subnets, and Route Tables.

## Architecture
Part of the 'App VPC' with CIDR block 10.0.1.0/24 in Availability Zone 'a'. It has a route table entry directing internet-bound traffic to the Internet Gateway.

## Operations

### Scaling
Subnet scaling involves expanding the CIDR block if it becomes exhausted or adding new subnets in different AZs. This is managed via Infrastructure as Code changes to `var.public_subnet_cidrs`.

### Monitoring
Monitor the IP address utilization within the subnet. Track traffic flow through associated route tables and security groups.

### Backup & Recovery
Subnet configurations are managed by Infrastructure as Code. If a subnet is accidentally deleted, re-running the IaC apply will recreate it.

### Common Issues
### Resources in Public Subnet AZ-A lack internet access
Ensure the route table associated with this subnet has a default route (0.0.0.0/0) pointing to the Internet Gateway. Verify security groups and Network ACLs allow necessary ingress/egress traffic.

## Rollback Procedure
1. Revert the Infrastructure as Code commit that modified or deleted the Public Subnet AZ-A.
2. Execute the IaC apply command.
3. Verify network connectivity for resources within the subnet.

## Contacts
Generated by CloudCrew AI — 2026-09-01